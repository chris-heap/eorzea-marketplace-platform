# API

FastAPI service for serving anomaly scores.

**Phase 5**

## Endpoints (planned)
- `GET /anomalies/{item_id}` — anomaly scores for an item
- `POST /score` — score a batch of listings
- `GET /health` — healthcheck
