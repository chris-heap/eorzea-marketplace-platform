# ML

PyTorch autoencoder for anomaly detection + MLflow experiment tracking.

**Phase 4**

## Components
- Feature engineering on mart tables (DuckDB)
- PyTorch autoencoder (price/volume time series)
- MLflow tracking + model registry
